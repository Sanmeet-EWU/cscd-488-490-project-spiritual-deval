@page "/CreateAccount"
@layout MainLayout
@rendermode InteractiveServer
@using SpiritualDevalApp.Data
@using Microsoft.AspNetCore.Identity
@using SpiritualDevalApp.Models.Entities

<PageTitle>Create Account</PageTitle>
<h2 class="heading">Enter your details</h2>
<div class="create-account-container">
    <div class="create-account-box">
        <EditForm Model="user" OnValidSubmit="HandleValidSubmit">
            <div class="form-group">
                <label for="userNameInput">User Name:</label>
                <InputText id="userNameInput" @bind-Value="user.UserName" class="form-control" />
            </div>
            <div class="form-group">
                <label for="passwordInput">Password:</label>
                <InputText id="passwordInput" @bind-Value="user.Password" class="form-control" type="password" />
            </div>
            <div class="form-group">
                <label for="firstNameInput">First Name:</label>
                <InputText id="firstNameInput" @bind-Value="user.FirstName" class="form-control" />
            </div>
            <div class="form-group">
                <label for="lastNameInput">Last Name:</label>
                <InputText id="lastNameInput" @bind-Value="user.LastName" class="form-control" />
            </div>
            <div class="form-group">
                <label for="emailInput">Email:</label>
                <InputText id="emailInput" @bind-Value="user.Email" class="form-control" />
            </div>
            <button type="submit" style="background-color: #3e8e41; color: white; border: none; padding: 10px 20px; font-size: 16px; font-weight: bold; border-radius: 5px; cursor: pointer;">Submit</button>

            
        </EditForm>
    </div>
</div>

@if (!string.IsNullOrEmpty(user.UserName))
{
    <p>Your username: @user.UserName</p>
}
@if (!string.IsNullOrEmpty(user.Password))
{
    <p>Your password: @user.Password</p>
}
@if (!string.IsNullOrEmpty(user.FirstName))
{
    <p>Your first name: @user.FirstName</p>
}
@if (!string.IsNullOrEmpty(user.LastName))
{
    <p>Your last name: @user.LastName</p>
}
@if (!string.IsNullOrEmpty(user.Email))
{
    <p>Your email: @user.Email</p>
}

@code {

    private User user = new User();

    // Injecting DbContext, PasswordHasher, and NavigationManager
    [Inject] 
    private AppDbContext DbContext { get; set; } = null!;

    [Inject]
    private IPasswordHasher<User> PasswordHasher { get; set; } = null!;

    [Inject]
    private NavigationManager NavigationManager { get; set; } = null!;

    private async Task HandleValidSubmit()
    {
        // Hash the password before storing it
        var hashedPassword = PasswordHasher.HashPassword(user, user.Password);

        var newUser = new UserAccount
        {
            UserName = user.UserName,
            Password = hashedPassword,
            Role = "User",  // Default role
            FirstName = user.FirstName,
            LastName = user.LastName,
            Email = user.Email
        };

        await DbContext.UserAccounts.AddAsync(newUser);
        await DbContext.SaveChangesAsync();

        // Redirect to the login page after successful account creation
        NavigationManager.NavigateTo("/login");
    }
}
<style>
    /* General page styles */
    body {
        background-color: #f4f7f6;
        font-family: 'Arial', sans-serif;
        margin: 0;
        height: 100vh;
    }

.create-account-container {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh; /* Ensures the container takes up at least the full height */
    background-color: #f4f7f6;
}


    .create-account-box {
        background-color: white;
        width: 100%;
        max-width: 600px; /* Increased width for a wider form */
        padding: 40px;
        border-radius: 10px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    }

    .heading {
        text-align: center;
        color: #556b2f; /* Olive green */
        font-family: 'Times New Roman', serif; /* Times New Roman font */
        font-size: 28px;
        font-weight: bold;
        margin-bottom: 30px; /* Adjust the bottom margin for spacing */
    }

    .form-group {
        margin-bottom: 20px;
    }

    .form-label {
        display: block;
        font-size: 14px;
        color: #333;
        margin-bottom: 5px;
    }

    .form-control {
        width: 100%;
        padding: 14px; /* Increased padding for a more spacious feel */
        border: 1px solid #ddd;
        border-radius: 5px;
        font-size: 16px;
        background-color: #f9f9f9;
        transition: border-color 0.3s ease-in-out;
    }

    .form-control:focus {
        border-color: #556b2f;
        outline: none;
    }

    .submit-btn {
        width: 100%;
        padding: 16px; /* Increased padding for a more button-like feel */
        background-color: #556b2f;
        color: white;
        font-size: 18px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

    .submit-btn:hover {
        background-color: #4b5320;
    }

    /* Extra styling for smaller text under form */
    p {
        color: #555;
        font-size: 14px;
        text-align: center;
        margin-top: 10px;
    }
</style>